source $U_ZDOTDIR/plugins/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh

#source $U_ZDOTDIR/plugins/auto-fu.zsh/auto-fu.zsh
#auto-fu-install

#zle-line-init () {auto-fu-init;}; zle -N zle-line-init

#zstyle ':completion:*' completer _oldlist _complete

#zle -N zle-keymap-select auto-fu-zle-keymap-select

#zstyle ':auto-fu:highlight' completion/one fg=blue,underline

# vim:ft=zsh
